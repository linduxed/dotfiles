#!/usr/bin/bash
s1=$(pacman -Q linux-lts | sed 's/linux-lts //' | sed 's/\.lts/-lts/')
s2=$(uname -r | sed 's/-lts$//')

echo "pacman -Q: $(pacman -Q linux-lts)"
echo "uname -r:  $(uname -r)"
echo ""

if [ "$s1" == "$s2" ]; then
  echo NO NEED
else
  echo REBOOT NECESSARY
fi
